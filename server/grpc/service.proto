syntax = "proto3";

package server;

// Define data coming into server
service PluginRoutes {

  // Get plugin with specific id
  rpc GetPlugin(Plugin) returns (Plugin) {}

  // Get plugin combination 
  rpc GetPlugins(Plugin) returns (*[]Plugin) {}
}

// A plugin is the incoming plugin id
message Plugin {
  string id
}